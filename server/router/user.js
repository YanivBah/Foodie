const express = require("express");
const router = new express.Router();
// const auth = require("../middleware/auth");
// const { functions } = require("../controller/user");

// router.get("/profile", auth, userDetails);

module.exports = router;